# Web API reference

Interactive reference for every endpoint, generated from the running service's
OpenAPI schema. For *how* to use the API (the submit → poll → fetch flow, auth,
and browser examples), read the [guide](web-api.md) first.

<swagger-ui src="openapi.json"/>
